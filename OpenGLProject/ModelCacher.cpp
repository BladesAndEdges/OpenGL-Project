#include "ModelCacher.h"
#include <assert.h>

bool ModelCacher::tryReadFromCache(const char * filePath)
{
	assert(filePath != nullptr);
	return false;
}

void ModelCacher::writeToCache(const char * fileName)
{
	assert(fileName != nullptr);
}
